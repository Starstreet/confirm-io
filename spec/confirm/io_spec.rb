require "spec_helper"

RSpec.describe Confirm::Io do
  it "has a version number" do
    expect(Confirm::Io::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
